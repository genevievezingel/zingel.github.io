# Icons

Run `bash vendor-icons.sh` from the repo root to populate this folder with the
brand SVGs used by the site (Python, Ruby, Rails, Git, PostgreSQL, Snowflake,
Databricks, dbt, AWS, SAS, Power BI, GitHub, LinkedIn, X, Medium).

Until then, the chips fall back to their text labels automatically.
